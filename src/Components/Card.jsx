import React from "react";

export default function Fathiza({ firstName, lastName }) {
    return (
        <div>
            <h1>{firstName}</h1>
            <p>{lastName}</p>
        </div>
    );
}
